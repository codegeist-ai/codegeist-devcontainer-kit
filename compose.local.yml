# compose.local.yml - local compose overrides for the devcontainer
#
# Why this exists:
# - Provides the default local compose override included by devcontainer.json.
# - Remains intentionally small because shared defaults live in docker-compose.yml;
#   add local or repository-specific overrides here when needed.
#
# Related files:
# - devcontainer.json
# - docker-compose.yml
# - initialize.sh
services: {}
